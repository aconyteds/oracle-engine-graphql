extend type Query {
  currentUser: User
}

extend type Mutation {
  login(input: LoginInput!): User
}

input LoginInput {
  email: String!
  password: String!
}

type User {
  id: ID!
  name: String!
  email: String!
}
